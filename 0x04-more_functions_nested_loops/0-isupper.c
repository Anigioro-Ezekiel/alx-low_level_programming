#include "main.h"

/**
 * _isupper(int c); - a function that checks for uppercase character.
 * @c: variable(alphabet)
 * Return: 1 if TRUE, 0 if FALSE
 */

int _isupper(int c);
{
	if (c >= 65 && c<=90)
	{
		Return(1);
	}
	Return(0);
}
